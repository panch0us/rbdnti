# 🚀 RBDNTI - Региональный банк данных научно-технической информации

## 📋 Оглавление
- [Обзор проекта](#-обзор-проекта)
- [Архитектура](#-архитектура)
- [Сервер разработки (с интернетом)](#-сервер-разработки-с-интернетом)
- [Сервер продакшена (без интернета)](#-сервер-продакшена-без-интернета)
- [Работа с базой данных](#️-работа-с-базой-данных)
- [Бэкапы и восстановление](#-бэкапы-и-восстановление)
- [Устранение неисправностей](#-устранение-неисправностей)
- [Часто задаваемые вопросы](#-часто-задаваемые-вопросы)
- [Шпаргалка для быстрого старта](#-шпаргалка-для-быстрого-старта)

---

## 🏗️ Обзор проекта

**RBDNTI** - это Django приложение для управления новостями и контентом, развернутое с использованием Docker, Gunicorn и Nginx.

### 🎯 Основные компоненты
- **Django** - веб-фреймворк
- **Gunicorn** - WSGI сервер
- **Nginx** - веб-сервер и прокси
- **SQLite** - база данных
- **Docker** - контейнеризация

---

## 📊 Архитектура

### Структура проекта
```
- rbdnti/
- ├── backend/           # Исходный код Django
- ├── data/              # Данные (БД, медиа, статика)
- ├── nginx/             # Конфигурация Nginx
- ├── manage.sh          # ГЛАВНЫЙ скрипт управления
- ├── develop.sh         # Скрипт разработки
- └── prepare-offline.sh # Подготовка для продакшена
```

### Сетевые порты
- **80** - Nginx (доступ к сайту)
- **8000** - Gunicorn (внутренний)

---

## 💻 Сервер разработки (с интернетом)
### Клонируем проект
```bash
git clone https://github.com/panch0us/rbdnti
cd rbdnti
```
### 🟢 Запустить в режиме разработки
```bash
./manage.sh dev
```
### 🔴 Остановить сервисы
```bash
./manage.sh stop
```
### 🧹 Полная очистка (останавливает и удаляет контейнеры)
```bash
./manage.sh clean
```
### 📊 Просмотр логов в реальном времени
```bash
./manage.sh logs
```
### 📋 Последние 20 строк логов
```bash
docker compose -f docker-compose.dev.yml logs web --tail=20
```
### 🔍 Проверить статус контейнеров
```bash
docker compose -f docker-compose.dev.yml ps
```
### 👤 Создать администратора
```bash
docker compose -f docker-compose.dev.yml exec web python /app/rbdnti/manage.py createsuperuser
```
### ⚙️ Создать файл настроек в корне проекта (.env)
```bash
SECRET_KEY=СКОПИРОВАТЬ-СВОЙ
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
```

---

## 💻 Сервер продакшена (без интернета)
### 📦 Первое развертывание
### Подготовка на интернет-сервере
### Создать оффлайн-пакет (получим файл: rbdnti-offline-YYYYMMDD.tar.gz)
```bash
./manage.sh build
```
### Развертывание на оффлайн-сервере
### Распаковать архив
```bash
tar -xzf rbdnti-offline-20241111.tar.gz
```
### Переименовать для понятности
```bash
mv rbdnti-offline-package rbdnti-v1
```
### Запустить развертывание
```bash
cd rbdnti-v1
./deploy.sh
```
## 🔄 Обновление версии
### Подготовка новой версии на интернет-сервере
```bash
./manage.sh build
```
### Развертывание на продакшене
### Распаковать новую версию
```bash
tar -xzf rbdnti-offline-20241115.tar.gz
mv rbdnti-offline-package rbdnti-v2
```
### Скопировать данные из старой версии
```bash
cp -r rbdnti-production/data rbdnti-v2/
```
### Запустить обновление
```bash
cd rbdnti-v2
./update.sh
```
### Проверить работу
```bash
curl http://localhost/
```
### Удалить старую версию (опционально)
```bash
rm -rf rbdnti-v1
```
## 🛠️ Управление на продакшене
### 🟢 Запуск сервисов
```bash
docker compose up -d
```
### 🔴 Остановка сервисов
```bash
docker compose down
```

### 📊 Статус контейнеров
```bash
docker compose ps
```
### 📋 Просмотр логов
```bash
docker compose logs web -f
```
### 👤 Создание суперпользователя
```bash
docker compose exec web python /app/rbdnti/manage.py createsuperuser
```

---

## 🗄️ Работа с базой данных
### 📤 Импорт базы с продакшена на разработку
### Остановить сервисы
```bash
./manage.sh stop
```
### Скопировать базу
```bash
cp /путь/к/бэкапу/db.sqlite3 data/db/
```
### Запустить заново
```bash
./manage.sh dev
```
## 📥 Экспорт базы с разработки на продакшен
### Создать бэкап базы
```bash
cp data/db/db.sqlite3 backup_db_$(date +%Y%m%d).sqlite3
```

## 🔍 Проверка целостности базы
### Проверить что база не повреждена
```bash
docker compose exec web python /app/rbdnti/manage.py check
```

---

## 💾 Бэкапы и восстановление
### 🔄 Автоматические бэкапы
### Добавить в crontab (ежедневно в 2:00)
```bash
0 2 * * * cd /home/administrator/projects/rbdnti && tar -czf /backups/rbdnti_$(date +\%Y\%m\%d).tar.gz data/
```
### 📦 Ручное создание бэкапа
### Полный бэкап данных
```bash
tar -czf rbdnti_backup_$(date +%Y%m%d_%H%M).tar.gz data/
```
### Только база данных
```bash
cp data/db/db.sqlite3 db_backup_$(date +%Y%m%d).sqlite3
```

### 🛟 Восстановление из бэкапа
### Остановить сервисы
```bash
docker compose down
```
### Восстановить данные
```bash
tar -xzf rbdnti_backup_20241111.tar.gz
```
### Запустить заново
```bash
docker compose up -d
```

---

## 🔧 Устранение неисправностей
### 🚨 Частые проблемы и решения
### Проблема: Ошибка 502 Bad Gateway
### Проверить статус контейнеров
```bash
docker compose ps
```
### Просмотреть логи
```bash
docker compose logs web
docker compose logs nginx
```
### Проверить что Gunicorn слушает порт
```bash
docker compose exec web netstat -tln | grep 8000
```
### Проблема: Неверные права доступа к data/
### Исправить права
```bash
sudo chown -R administrator:administrator data/
chmod 755 data/ data/db/ data/media/ data/staticfiles/
```
### Проблема: Контейнеры не запускаются
### Полная перезагрузка
```bash
./manage.sh clean
./manage.sh dev
```
### Проблема: Миграции не применяются
### Принудительно создать и применить миграции
```bash
docker compose exec web python /app/rbdnti/manage.py makemigrations news_site
docker compose exec web python /app/rbdnti/manage.py migrate
```
### 📊 Диагностика
### Проверить использование диска
```bash
docker system df
```
### Показать все контейнеры (включая остановленные)
```bash
docker ps -a
```
### Показать все образы
```bash
docker images
```
### Очистить неиспользуемые данные
```bash
docker system prune -f
```

---

## ❓ Часто задаваемые вопросы
### Как проверить что сайт работает? (Должен вернуть HTTP 200)
```bash
curl -I http://localhost/
```
### Где хранятся данные пользователей?
```bash
База данных: data/db/db.sqlite3
Медиа файлы: data/media/
Статические файлы: data/staticfiles/
```
### Как сменить пароль администратора?
```bash
docker compose exec web python /app/rbdnti/manage.py changepassword username
```
### Что делать если забыл пароль от админки?
### Создать нового суперпользователя
```bash
docker compose exec web python /app/rbdnti/manage.py createsuperuser
```
### Как посмотреть логи за последний час?
```bash
docker compose logs web --since 1h
```
### Как перезапустить только один сервис?
```bash
docker compose restart web
# или
docker compose restart nginx
```

---

## 🎯 Шпаргалка для быстрого старта
### Для разработки:
```bash
./manage.sh dev     # 🟢 Запустить
./manage.sh stop    # 🔴 Остановить  
./manage.sh logs    # 📊 Логи
./manage.sh build   # 📦 Собрать для прода
```
### Для продакшена:
```bash
./deploy.sh         # 🚀 Первое развертывание
./update.sh         # 🔄 Обновление версии
docker compose ps   # 📋 Что запущено
docker compose down # ⏹️ Остановить
```
### Для бэкапов:
### Создать бэкап
```bash
tar -czf backup_$(date +%Y%m%d).tar.gz data/
```
### Восстановить
```bash
tar -xzf backup_20241111.tar.gz
```
### Создать бэкап
```bash
tar -czf backup_$(date +%Y%m%d).tar.gz data/
```
### Восстановить
```bash
tar -xzf backup_20241111.tar.gz
```
## Если у вас возникли проблемы:
```bash
Проверьте логи: ./manage.sh logs
Убедитесь что все контейнеры запущены: docker compose ps
Проверьте права доступа к папке data/
Создайте бэкап перед любыми изменениями
Важно: Всегда сохраняйте старую версию до тех пор, пока не убедитесь что новая работает правильно!
``` 